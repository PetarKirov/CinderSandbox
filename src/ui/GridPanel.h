#pragma once;

#include "Panel.h"

namespace zlx
{
    namespace ui
    {
        struct GridColumnsDefinition
        {
			//Should use initializer_list<int>
        };

        struct GridRowsDefinition
        {
			//Should use initializer_list<int>
        };

        class GridPanel
        {

        };
    }
}